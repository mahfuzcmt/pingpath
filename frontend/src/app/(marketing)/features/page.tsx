import { FeaturesPage } from "@/components/landing/pages/FeaturesPage";

export const metadata = {
  title: "GPS Tracking Features — MotoLink Fleet Management",
  description: "Real-time GPS tracking, fuel monitoring, geofencing, route replay, engine immobilizer, and smart alerts. Complete fleet management solution.",
};

export default function Features() {
  return <FeaturesPage />;
}
